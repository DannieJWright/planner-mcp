{
  "name": "@planner/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "planner-mcp": "dist/main.js"
  },
  "scripts": {
    "prebuild": "npm run build --workspace=@planner/api",
    "predev": "npm run build --workspace=@planner/api",
    "pretest": "npm run build --workspace=@planner/api",
    "pretypecheck": "npm run build --workspace=@planner/api",
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit --passWithNoTests",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "vitest run tests/e2e"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "@planner/api": "*",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
